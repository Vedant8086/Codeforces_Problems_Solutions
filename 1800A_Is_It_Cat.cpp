#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        string s;
        cin >> s;
        
        for(int i = 0; i < n; i++) {
            if() {
                
            }
            cat.insert({tolower(s[i]), i});
        }
        for(auto p : cat) {
            if(p.first == 'c' || p.first == 'a' || p.first == 't') {
                // Do nothing
            }
        }
    }
    return 0;
}